import 'package:flutter/material.dart';

class CusProgress extends StatelessWidget {
  const CusProgress({super.key});

  @override
  Widget build(BuildContext context) =>
      Center(child: CircularProgressIndicator());
}
